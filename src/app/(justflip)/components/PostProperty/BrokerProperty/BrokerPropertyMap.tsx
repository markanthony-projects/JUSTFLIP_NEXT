"use client";
// BrokerPropertyMap.tsx — wraps the shared PublishPropertyMap modal
// so that BrokerPropertyClient can import it from its own folder.

export { default } from '../PublishProperty/PublishPropertyMap';
